package mattmunz.cli.commandline;

/**
 * A command line is comprised of a command and an ordered list of space-delimited arguments. 
 * Both options and parameters are kinds of arguments. Options may be either short (one 
 * dash, one character) or long (two dashes, multiple characters).
 * Arguments may be optional or required.
 */
interface Argument {}
